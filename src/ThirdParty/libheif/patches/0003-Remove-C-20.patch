From 5d7f6c96fc42d112deabc63706d3ebf191c81730 Mon Sep 17 00:00:00 2001
From: Peter Zhigalov <peter.zhigalov@gmail.com>
Date: Sat, 23 Nov 2024 20:17:00 +0700
Subject: [PATCH 3/4] Remove C++20

---
 heifio/decoder_raw.cc                         | 12 ++-
 libheif/bitstream.cc                          | 19 -----
 libheif/codecs/avc_boxes.cc                   |  3 +-
 .../unc_decoder_block_component_interleave.cc | 12 ++-
 .../unc_decoder_block_pixel_interleave.cc     | 12 ++-
 ..._decoder_bytealign_component_interleave.cc |  1 -
 .../uncompressed/unc_decoder_legacybase.cc    | 17 +++--
 .../unc_encoder_component_interleave.cc       | 12 ++-
 libheif/context.cc                            |  6 +-
 libheif/image-items/grid.cc                   |  2 +-
 libheif/image-items/iden.cc                   |  2 +-
 libheif/image-items/image_item.cc             |  4 +-
 libheif/image-items/overlay.cc                |  2 +-
 libheif/image-items/tiled.cc                  |  8 +-
 libheif/nclx.h                                |  4 +-
 libheif/security_limits.cc                    | 76 ++++++++++++++-----
 16 files changed, 121 insertions(+), 71 deletions(-)

diff --git a/heifio/decoder_raw.cc b/heifio/decoder_raw.cc
index a2ed77ae..836b4388 100644
--- a/heifio/decoder_raw.cc
+++ b/heifio/decoder_raw.cc
@@ -27,13 +27,21 @@
 #include "decoder_raw.h"
 #include <libheif/heif.h>
 #include <libheif/heif_uncompressed.h>
-#include <bit>
 #include <cstdio>
 #include <cstring>
 #include <vector>
 #include <memory>
 #include <utility>
 
+#include <QtGlobal>
+static constexpr int std_endian_big     = 4321;
+static constexpr int std_endian_little  = 1234;
+#if defined(Q_BYTE_ORDER) && (Q_BYTE_ORDER == Q_BIG_ENDIAN)
+static constexpr int std_endian_native  = std_endian_big;
+#else
+static constexpr int std_endian_native  = std_endian_little;
+#endif
+
 static struct heif_error heif_error_ok = {heif_error_Ok, heif_suberror_Unspecified, "Success"};
 
 
@@ -170,7 +178,7 @@ heif_error loadRAW(const char* filename, const RawImageParameters& params, Input
 
   // Byte-swap from big-endian to host byte order if needed
   if (params.big_endian && bytes_per_pixel > 1) {
-    if constexpr (std::endian::native == std::endian::little) {
+    if (std_endian_native == std_endian_little) {
       byte_swap_buffer(buffer.data(), static_cast<size_t>(width) * height, bytes_per_pixel);
     }
   }
diff --git a/libheif/bitstream.cc b/libheif/bitstream.cc
index be29548e..f9fe571f 100644
--- a/libheif/bitstream.cc
+++ b/libheif/bitstream.cc
@@ -26,12 +26,6 @@
 
 #include "common_utils.h"
 
-#if !defined(HAVE_BIT)
-#include <type_traits>
-#else
-#include <bit>
-#endif
-
 #define MAX_UVLC_LEADING_ZEROS 20
 
 
@@ -352,12 +346,6 @@ int64_t BitstreamRange::read64s()
 
 float BitstreamRange::read_float32()
 {
-#if __cpp_lib_bit_cast >= 201806L
-  uint32_t i = read32();
-  return std::bit_cast<float>(i); // this works directly on the value layout, thus we do not have to worry about memory layout
-#else
-  // compiler too old to support bit_cast
-
   // TODO: I am not sure this works everywhere as there seem to be systems where
   //       the float byte order is different from the integer endianness
   //       https://en.wikipedia.org/wiki/Endianness#Floating_point
@@ -365,24 +353,17 @@ float BitstreamRange::read_float32()
   float f;
   memcpy(&f, &i, sizeof(float));
   return f;
-#endif
 }
 
 
 void StreamWriter::write_float32(float v)
 {
-#if __cpp_lib_bit_cast >= 201806L
-  write32(std::bit_cast<uint32_t>(v)); // this works directly on the value layout, thus we do not have to worry about memory layout
-#else
-  // compiler too old to support bit_cast
-
   // TODO: I am not sure this works everywhere as there seem to be systems where
   //       the float byte order is different from the integer endianness
   //       https://en.wikipedia.org/wiki/Endianness#Floating_point
   uint32_t i;
   memcpy(&i, &v, sizeof(float));
   write32(i);
-#endif
 }
 
 
diff --git a/libheif/codecs/avc_boxes.cc b/libheif/codecs/avc_boxes.cc
index 8d7d82ef..33b2b404 100644
--- a/libheif/codecs/avc_boxes.cc
+++ b/libheif/codecs/avc_boxes.cc
@@ -371,7 +371,8 @@ Error parse_sps_for_avcC_configuration(const uint8_t* sps, size_t size,
   uint32_t value;
   if (!reader.get_uvlc(&value)) { return invalidUVLC; } // SPS ID
 
-  if (std::set<int>{100, 110, 122, 244, 44, 83, 86}.contains(config->AVCProfileIndication)) {
+  static const std::set<int> s{100, 110, 122, 244, 44, 83, 86};
+  if (s.find(config->AVCProfileIndication) != s.cend()) {
     if (!reader.get_uvlc(&value)) {
       return invalidUVLC;
     }
diff --git a/libheif/codecs/uncompressed/unc_decoder_block_component_interleave.cc b/libheif/codecs/uncompressed/unc_decoder_block_component_interleave.cc
index fd2f7f72..f68c2a84 100644
--- a/libheif/codecs/uncompressed/unc_decoder_block_component_interleave.cc
+++ b/libheif/codecs/uncompressed/unc_decoder_block_component_interleave.cc
@@ -24,11 +24,19 @@
 #include "unc_types.h"
 #include "error.h"
 
-#include <bit>
 #include <cassert>
 #include <cstring>
 #include <vector>
 
+#include <QtGlobal>
+static constexpr int std_endian_big     = 4321;
+static constexpr int std_endian_little  = 1234;
+#if defined(Q_BYTE_ORDER) && (Q_BYTE_ORDER == Q_BIG_ENDIAN)
+static constexpr int std_endian_native  = std_endian_big;
+#else
+static constexpr int std_endian_native  = std_endian_little;
+#endif
+
 
 unc_decoder_block_component_interleave::unc_decoder_block_component_interleave(
     uint32_t width, uint32_t height,
@@ -159,7 +167,7 @@ Error unc_decoder_block_component_interleave::decode_tile(const std::vector<uint
           }
           else {
             // Store in native endian (2 bytes).
-            if constexpr (std::endian::native == std::endian::little) {
+            if (std_endian_native == std_endian_little) {
               dst[0] = static_cast<uint8_t>(value & 0xFF);
               dst[1] = static_cast<uint8_t>((value >> 8) & 0xFF);
             }
diff --git a/libheif/codecs/uncompressed/unc_decoder_block_pixel_interleave.cc b/libheif/codecs/uncompressed/unc_decoder_block_pixel_interleave.cc
index 9ad9b006..e4921029 100644
--- a/libheif/codecs/uncompressed/unc_decoder_block_pixel_interleave.cc
+++ b/libheif/codecs/uncompressed/unc_decoder_block_pixel_interleave.cc
@@ -24,11 +24,19 @@
 #include "unc_types.h"
 #include "error.h"
 
-#include <bit>
 #include <cassert>
 #include <cstring>
 #include <vector>
 
+#include <QtGlobal>
+static constexpr int std_endian_big     = 4321;
+static constexpr int std_endian_little  = 1234;
+#if defined(Q_BYTE_ORDER) && (Q_BYTE_ORDER == Q_BIG_ENDIAN)
+static constexpr int std_endian_native  = std_endian_big;
+#else
+static constexpr int std_endian_native  = std_endian_little;
+#endif
+
 
 unc_decoder_block_pixel_interleave::unc_decoder_block_pixel_interleave(
     uint32_t width, uint32_t height,
@@ -174,7 +182,7 @@ Error unc_decoder_block_pixel_interleave::decode_tile(const std::vector<uint8_t>
         }
         else {
           // Store in native endian (2 bytes).
-          if constexpr (std::endian::native == std::endian::little) {
+          if (std_endian_native == std_endian_little) {
             dst[0] = static_cast<uint8_t>(value & 0xFF);
             dst[1] = static_cast<uint8_t>((value >> 8) & 0xFF);
           }
diff --git a/libheif/codecs/uncompressed/unc_decoder_bytealign_component_interleave.cc b/libheif/codecs/uncompressed/unc_decoder_bytealign_component_interleave.cc
index 7c557703..f2180ffa 100644
--- a/libheif/codecs/uncompressed/unc_decoder_bytealign_component_interleave.cc
+++ b/libheif/codecs/uncompressed/unc_decoder_bytealign_component_interleave.cc
@@ -24,7 +24,6 @@
 #include "unc_types.h"
 #include "error.h"
 
-#include <bit>
 #include <cassert>
 #include <cstring>
 #include <vector>
diff --git a/libheif/codecs/uncompressed/unc_decoder_legacybase.cc b/libheif/codecs/uncompressed/unc_decoder_legacybase.cc
index 8458becc..114f50d7 100644
--- a/libheif/codecs/uncompressed/unc_decoder_legacybase.cc
+++ b/libheif/codecs/uncompressed/unc_decoder_legacybase.cc
@@ -24,12 +24,6 @@
 #include <cassert>
 #include <utility>
 
-#if ((defined(__GNUC__) && !defined(__clang__) && !defined(__INTEL_COMPILER) && !defined(__PGI)) && __GNUC__ < 9) || (defined(__clang__) && __clang_major__ < 10)
-#include <type_traits>
-#else
-#include <bit>
-#endif
-
 #include "common_utils.h"
 #include "context.h"
 #include "compression.h"
@@ -41,6 +35,15 @@
 #include "unc_decoder_legacybase.h"
 #include "codecs/decoder.h"
 
+#include <QtGlobal>
+static constexpr int std_endian_big     = 4321;
+static constexpr int std_endian_little  = 1234;
+#if defined(Q_BYTE_ORDER) && (Q_BYTE_ORDER == Q_BIG_ENDIAN)
+static constexpr int std_endian_native  = std_endian_big;
+#else
+static constexpr int std_endian_native  = std_endian_little;
+#endif
+
 
 unc_decoder_legacybase::unc_decoder_legacybase(uint32_t width, uint32_t height,
                                                const std::shared_ptr<const Box_cmpd>& cmpd,
@@ -75,7 +78,7 @@ void unc_decoder_legacybase::memcpy_to_native_endian(uint8_t* dst, uint32_t valu
     *dst = static_cast<uint8_t>(value);
     return;
   }
-  else if (std::endian::native == std::endian::big) {
+  else if (std_endian_native == std_endian_big) {
     for (uint32_t i = 0; i < bytes_per_sample; i++) {
       dst[bytes_per_sample - 1 - i] = static_cast<uint8_t>((value >> (i * 8)) & 0xFF);
     }
diff --git a/libheif/codecs/uncompressed/unc_encoder_component_interleave.cc b/libheif/codecs/uncompressed/unc_encoder_component_interleave.cc
index 4c88d71d..c99eb17d 100644
--- a/libheif/codecs/uncompressed/unc_encoder_component_interleave.cc
+++ b/libheif/codecs/uncompressed/unc_encoder_component_interleave.cc
@@ -20,12 +20,20 @@
 
 #include "unc_encoder_component_interleave.h"
 
-#include <bit>
 #include <cstring>
 
 #include "image/pixelimage.h"
 #include "unc_boxes.h"
 
+#include <QtGlobal>
+static constexpr int std_endian_big     = 4321;
+static constexpr int std_endian_little  = 1234;
+#if defined(Q_BYTE_ORDER) && (Q_BYTE_ORDER == Q_BIG_ENDIAN)
+static constexpr int std_endian_native  = std_endian_big;
+#else
+static constexpr int std_endian_native  = std_endian_little;
+#endif
+
 
 bool unc_encoder_factory_component_interleave::can_encode(const std::shared_ptr<const HeifPixelImage>& image,
                                                           const heif_encoding_options& options) const
@@ -136,7 +144,7 @@ unc_encoder_component_interleave::unc_encoder_component_interleave(const std::sh
 
   m_uncC->set_interleave_type(interleave_mode_component);
   if (m_use_memcpy) {
-    m_uncC->set_components_little_endian(std::endian::native == std::endian::little);
+    m_uncC->set_components_little_endian(std_endian_native == std_endian_little);
   }
   else {
     // we use dense packing
diff --git a/libheif/context.cc b/libheif/context.cc
index 37c9e30a..962c44b7 100644
--- a/libheif/context.cc
+++ b/libheif/context.cc
@@ -342,7 +342,7 @@ std::shared_ptr<const ImageItem> HeifContext::get_primary_image(bool return_erro
 
 bool HeifContext::is_image(heif_item_id ID) const
 {
-  return m_all_images.contains(ID);
+  return m_all_images.find(ID) != m_all_images.cend();
 }
 
 
@@ -1383,7 +1383,7 @@ Result<heif_item_id> HeifContext::find_first_coded_image_id(heif_item_id id) con
       id = image_references[0];
     }
     else {
-      if (!m_all_images.contains(id)) {
+      if (m_all_images.find(id) == m_all_images.cend()) {
         std::stringstream sstr;
         sstr << "Image item " << id << " referenced, but it does not exist\n";
 
@@ -1409,7 +1409,7 @@ Result<std::shared_ptr<HeifPixelImage>> HeifContext::decode_image(heif_item_id I
                                                                   std::set<heif_item_id> processed_ids) const
 {
   std::shared_ptr<ImageItem> imgitem;
-  if (m_all_images.contains(ID)) {
+  if (m_all_images.find(ID) != m_all_images.cend()) {
     imgitem = m_all_images.find(ID)->second;
   }
 
diff --git a/libheif/image-items/grid.cc b/libheif/image-items/grid.cc
index b6a68bc1..4460a48c 100644
--- a/libheif/image-items/grid.cc
+++ b/libheif/image-items/grid.cc
@@ -211,7 +211,7 @@ Result<std::shared_ptr<HeifPixelImage>> ImageItem_Grid::decode_compressed_image(
                                                                                 bool decode_tile_only, uint32_t tile_x0, uint32_t tile_y0,
                                                                                 std::set<heif_item_id> processed_ids) const
 {
-  if (processed_ids.contains(get_id())) {
+  if (processed_ids.find(get_id()) != processed_ids.cend()) {
     return Error{heif_error_Invalid_input,
                  heif_suberror_Unspecified,
                  "'iref' has cyclic references"};
diff --git a/libheif/image-items/iden.cc b/libheif/image-items/iden.cc
index 8050af27..2a026c27 100644
--- a/libheif/image-items/iden.cc
+++ b/libheif/image-items/iden.cc
@@ -39,7 +39,7 @@ Result<std::shared_ptr<HeifPixelImage>> ImageItem_iden::decode_compressed_image(
                                                                                 bool decode_tile_only, uint32_t tile_x0, uint32_t tile_y0,
                                                                                 std::set<heif_item_id> processed_ids) const
 {
-  if (processed_ids.contains(get_id())) {
+  if (processed_ids.find(get_id()) != processed_ids.cend()) {
     return Error{heif_error_Invalid_input,
                  heif_suberror_Unspecified,
                  "'iref' has cyclic references"};
diff --git a/libheif/image-items/image_item.cc b/libheif/image-items/image_item.cc
index e9cdb585..73e4f53d 100644
--- a/libheif/image-items/image_item.cc
+++ b/libheif/image-items/image_item.cc
@@ -875,7 +875,7 @@ Result<std::shared_ptr<HeifPixelImage>> ImageItem::decode_image(const heif_decod
   // The matching insert lives inside decode_compressed_image() of derived
   // items (grid/overlay/iden), so the current item is in processed_ids only
   // when called from one of its own descendants.
-  if (processed_ids.contains(m_id)) {
+  if (processed_ids.find(m_id) != processed_ids.cend()) {
     return Error{heif_error_Invalid_input,
                  heif_suberror_Unspecified,
                  "'iref' has cyclic references"};
@@ -1205,7 +1205,7 @@ Result<std::shared_ptr<HeifPixelImage>> ImageItem::decode_compressed_image(const
                                                                            bool decode_tile_only, uint32_t tile_x0, uint32_t tile_y0,
                                                                            std::set<heif_item_id> processed_ids) const
 {
-  if (processed_ids.contains(m_id)) {
+  if (processed_ids.find(m_id) != processed_ids.cend()) {
     return Error{heif_error_Invalid_input,
                  heif_suberror_Unspecified,
                  "'iref' has cyclic references"};
diff --git a/libheif/image-items/overlay.cc b/libheif/image-items/overlay.cc
index 4fe25786..7d63320b 100644
--- a/libheif/image-items/overlay.cc
+++ b/libheif/image-items/overlay.cc
@@ -291,7 +291,7 @@ Result<std::shared_ptr<HeifPixelImage>> ImageItem_Overlay::decode_compressed_ima
 Result<std::shared_ptr<HeifPixelImage>> ImageItem_Overlay::decode_overlay_image(const heif_decoding_options& options,
                                                                                 std::set<heif_item_id> processed_ids) const
 {
-  if (processed_ids.contains(get_id())) {
+  if (processed_ids.find(get_id()) != processed_ids.cend()) {
     return Error{heif_error_Invalid_input,
                  heif_suberror_Unspecified,
                  "'iref' has cyclic references"};
diff --git a/libheif/image-items/tiled.cc b/libheif/image-items/tiled.cc
index 0c504965..edb56a08 100644
--- a/libheif/image-items/tiled.cc
+++ b/libheif/image-items/tiled.cc
@@ -644,10 +644,10 @@ Error ImageItem_Tiled::initialize_decoder()
         }
       }
     }
-    std::erase_if(props, [](const std::shared_ptr<Box>& box) {
+    props.erase(std::remove_if(props.begin(), props.end(), [](const std::shared_ptr<Box>& box) {
       uint32_t type = box->get_short_type();
       return type == fourcc("icef") || type == fourcc("sbpm") || type == fourcc("snuc");
-    });
+    }), props.end());
 #endif
 
     m_tile_item->set_properties(props);
@@ -671,10 +671,10 @@ Error ImageItem_Tiled::initialize_decoder()
         }
       }
     }
-    std::erase_if(tile_properties, [](const std::shared_ptr<Box>& box) {
+    tile_properties.erase(std::remove_if(tile_properties.begin(), tile_properties.end(), [](const std::shared_ptr<Box>& box) {
       uint32_t type = box->get_short_type();
       return type == fourcc("icef") || type == fourcc("sbpm") || type == fourcc("snuc");
-    });
+    }), tile_properties.end());
 #endif
 
     bool have_ispe = false;
diff --git a/libheif/nclx.h b/libheif/nclx.h
index 1192cd79..6c25aaee 100644
--- a/libheif/nclx.h
+++ b/libheif/nclx.h
@@ -125,9 +125,9 @@ struct nclx_profile
   uint16_t m_matrix_coefficients = heif_matrix_coefficients_unspecified;
   bool m_full_range_flag = true;
 
-  bool operator==(const nclx_profile& b) const = default;
+  bool operator==(const nclx_profile& b) const { return m_colour_primaries == b.m_colour_primaries && m_transfer_characteristics == b.m_transfer_characteristics && m_matrix_coefficients == b.m_matrix_coefficients && m_full_range_flag == b.m_full_range_flag; }
 
-  bool operator!=(const nclx_profile& b) const = default;
+  bool operator!=(const nclx_profile& b) const { return !(*this == b); }
 
   static nclx_profile undefined() { return {}; }
 
diff --git a/libheif/security_limits.cc b/libheif/security_limits.cc
index 1a85b3ed..daaf2d17 100644
--- a/libheif/security_limits.cc
+++ b/libheif/security_limits.cc
@@ -25,50 +25,84 @@
 
 
 heif_security_limits global_security_limits{
-    .version = 4,
+    /*.version =*/ 4,
 
     // --- version 1
 
     // Artificial limit to avoid allocating too much memory.
     // 32768^2 = 1.5 GB as YUV-4:2:0 or 4 GB as RGB32
-    .max_image_size_pixels = 32768 * 32768,
-    .max_number_of_tiles = 4096 * 4096,
-    .max_bayer_pattern_pixels = 16 * 16,
-    .max_items = 1000,
+    /*.max_image_size_pixels =*/ 32768 * 32768,
+    /*.max_number_of_tiles =*/ 4096 * 4096,
+    /*.max_bayer_pattern_pixels =*/ 16 * 16,
+    /*.max_items =*/ 1000,
 
-    .max_color_profile_size = 100 * 1024 * 1024, // 100 MB
-    .max_memory_block_size = UINT64_C(4) * 1024 * 1024 * 1024,  // 4 GB
+    /*.max_color_profile_size =*/ 100 * 1024 * 1024, // 100 MB
+    /*.max_memory_block_size =*/ UINT64_C(4) * 1024 * 1024 * 1024,  // 4 GB
 
-    .max_components = 256,
-    .max_iloc_extents_per_item = 32,
-    .max_size_entity_group = 64,
+    /*.max_components =*/ 256,
+    /*.max_iloc_extents_per_item =*/ 32,
+    /*.max_size_entity_group =*/ 64,
 
-    .max_children_per_box = 100,
+    /*.max_children_per_box =*/ 100,
 
     // --- version 2
 
-    .max_total_memory = UINT64_C(4) * 1024 * 1024 * 1024,  // 4 GB
-    .max_sample_description_box_entries = 1024,
-    .max_sample_group_description_box_entries = 1024,
+    /*.max_total_memory =*/ UINT64_C(4) * 1024 * 1024 * 1024,  // 4 GB
+    /*.max_sample_description_box_entries =*/ 1024,
+    /*.max_sample_group_description_box_entries =*/ 1024,
 
     // --- version 3
 
-    .max_sequence_frames = 18'000'000,  // 100 hours at 50 fps
-    .max_number_of_file_brands = 1000,
+    /*.max_sequence_frames =*/ 100 * 60 * 60 * 50,  // 100 hours at 50 fps
+    /*.max_number_of_file_brands =*/ 1000,
 
     // --- version 4
 
-    .max_bad_pixels = 1000,
+    /*.max_bad_pixels =*/ 1000,
 
-    .max_iso23001_17_pixel_size_bytes = 256,
+    /*.max_iso23001_17_pixel_size_bytes =*/ 256,
 
-    .parent = nullptr
+    /*.parent =*/ nullptr
 };
 
 
 heif_security_limits disabled_security_limits{
-    .version = 4,
-    .parent = nullptr
+    /*.version =*/ 4,
+
+    // --- version 1
+
+    /*.max_image_size_pixels =*/ 0,
+    /*.max_number_of_tiles =*/ 0,
+    /*.max_bayer_pattern_pixels =*/ 0,
+    /*.max_items =*/ 0,
+
+    /*.max_color_profile_size =*/ 0,
+    /*.max_memory_block_size =*/ 0,
+
+    /*.max_components =*/ 0,
+    /*.max_iloc_extents_per_item =*/ 0,
+    /*.max_size_entity_group =*/ 0,
+
+    /*.max_children_per_box =*/ 0,
+
+    // --- version 2
+
+    /*.max_total_memory =*/ 0,
+    /*.max_sample_description_box_entries =*/ 0,
+    /*.max_sample_group_description_box_entries =*/ 0,
+
+    // --- version 3
+
+    /*.max_sequence_frames =*/ 0,  // 100 hours at 50 fps
+    /*.max_number_of_file_brands =*/ 0,
+
+    // --- version 4
+
+    /*.max_bad_pixels =*/ 0,
+
+    /*.max_iso23001_17_pixel_size_bytes =*/ 0,
+
+    /*.parent =*/ nullptr
 };
 
 
-- 
2.47.3

